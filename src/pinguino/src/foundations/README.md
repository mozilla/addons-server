# foundations

Shared web foundations for the whole system: design tokens, base components, and utilities that product areas (like `../devhub`) build on. Mostly empty for now; it marks where that reusable layer will live.
