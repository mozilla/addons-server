// Importing the entry point registers every acorn moz-* custom element; the CSS
// import adds the document-level design tokens.
import '@mozilla/acorn-web-components';
import '@mozilla/acorn-web-components/foundation.css';
