# acorn is published to GitHub Packages while in alpha; the @mozilla scope
# resolves there. GitHub Packages requires auth even for public packages, so the
# token comes from the NODE_AUTH_TOKEN env var (never commit a token here).
@mozilla:registry=https://npm.pkg.github.com
//npm.pkg.github.com/:_authToken=${NODE_AUTH_TOKEN}
