version: "2.3"

services:
  web:
    depends_on:
      - customs
      - wat

  customs:
    build:
      context: ./private/addons-customs-scanner
    environment:
      - ALLOWED_ORIGIN=http://olympia.test
      - PORT=10101
    links:
      - "nginx:olympia.test"

  wat:
    build:
      context: ./private/addons-wat
    environment:
      - ALLOWED_ORIGIN=http://olympia.test
      - PORT=10102
    links:
      - "nginx:olympia.test"
